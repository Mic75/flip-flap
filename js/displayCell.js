/**
 * 
 * @returns {undefined}
 */
define(["!..shaders/flipflap.fs","!../shaders/flipflap.vs", "glMatrix"], function(fs, vs, glMatrix) {

    /**
     * 
     * @param {Object} spec
     * @param {type} my
     * @returns {undefined}
     */
    var cell = function(spec, my) {
        
        /*
         * Private members 
         */
        var that = {};
        my = my || {};
        
        /*
         * Intern init
         */

        /*
         * Public interface
         */

        return that;

    };

    return cell;
});





